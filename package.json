{
  "scripts": {
    "start": "parcel serve src/_index.html --port 8088 --no-cache --out-file index.html",
    "build": "parcel build src/_index.html --out-file index.html"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.7",
    "parcel-plugin-elm": "^0.1.3-alpha"
  },
  "dependencies": {
    "elm": "^0.18.0"
  }
}
