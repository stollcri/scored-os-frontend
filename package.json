{
  "scripts": {
    "build": "elm make src/example.elm --output elm.js",
    "watch": "chokidar '**/*.elm' -c 'elm make src/Main.elm --output elm.compiled.js' --initial"
  },
  "devDependencies": {
    "chokidar-cli": "^1.2.0"
  }
}
